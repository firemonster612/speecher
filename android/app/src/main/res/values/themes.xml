<resources>
    <!-- Only paints the window before Compose draws; SpeecherTheme owns every colour after that. -->
    <style name="Theme.Speecher" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FAF9F4</item>
    </style>
</resources>
